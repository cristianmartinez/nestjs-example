type Product {
    id: String!
    name: String!
    price: Int!
    rate: Int!
    formattedPrice: String
}

type Query {
    product(id: Int!): Product
    products: [Product]!
    bestProducts: [Product]!
}
